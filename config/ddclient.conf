######################################################################
##
## Define default global variables with lines like:
## 	var=value [, var=value]*
## These values will be used for each following host unless overridden
## with a local variable definition.
##
## Define local variables for one or more hosts with:
## 	var=value [, var=value]* host.and.domain[,host2.and.domain...]
##
## Lines can be continued on the following line by ending the line
## with a \
##
##
## Warning: not all supported routers or dynamic DNS services
##          are mentioned here.
##
######################################################################
daemon=30				            # check every 30 seconds
syslog=yes				            # log update msgs to syslog
mail=root				            # mail all msgs to root
mail-failure=root		            # mail failed update msgs to root
pid=@runstatedir@/ddclient.pid		# record PID in file.
ssl=yes					            # use ssl-support.  Works with
						            # ssl-library
# postscript=script		            # run script after updating.  The
						            # new IP is added as argument.

# gavanlamb
use=web, web=dynamicdns.park-your-domain.com/getip
protocol=namecheap
server=dynamicdns.park-your-domain.com
login=gavanlamb.com
password=your dynamic dns password
cadvisor,console.minio,current.teamcity,latest.teamcity,filebrowser,grafana,homeassistant,minio,nexus,pgadmin,portainer,prometheus,sonarqube,traefik

#bootstrappers.co
use=web, web=dynamicdns.park-your-domain.com/getip
protocol=namecheap
server=dynamicdns.park-your-domain.com
login=bootstrappers.co
password=your dynamic dns password
www

#flippantlamb.com
use=web, web=dynamicdns.park-your-domain.com/getip
protocol=namecheap
server=dynamicdns.park-your-domain.com
login=flippantlamb.com
password=your dynamic dns password
www
